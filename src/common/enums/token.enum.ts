export enum TokenEnum {
  Access = "access",
  Refresh = "refresh",
}

export enum SignatureLevelEnum {
  Bearer = "Bearer",
  System = "System",
}

export enum LogoutEnum {
  Only = "only",
  All = "all",
}
