export enum ProviderEnum {
  Google = 'google',
  System = 'system',
}

export enum GenderEnum {
  Male = 'male',
  Female = 'female',
}

export enum RoleEnum {
  User = 'user',
  Admin = 'admin',
}

export enum BanEnum {
  Banned = "banned",
  UnBanned = "unbanned",
}
